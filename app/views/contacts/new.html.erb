<div class="container">
  <div class="row">
    <h3 class="text-center">Contact us</h3>
    
    <div class="col-md-4 col-md-offset-4">
      <%= flash[:notice] %>
      <div class="well">
        <%= form_for @contact do |f| %>
          <!-- the instance variable links this to contacts_controller.rb, pointing the form to the blank object -->
          <div class="form-group">
            <%= f.label :name %>
            <%= f.text_field :name, class: 'form-control' %>
          </div>
          
          <div class="form-group">
            <%= f.label :email %>
            <%= f.text_field :email, class: 'form-control' %>
          </div>
          
          <div class="form-group">
            <%= f.label :comments %>
            <%= f.text_area :comments, class: 'form-control' %>
          </div>
        
        <%= f.submit 'Submit', class: 'btn btn-default' %>
        
        <% end %>
      </div>
    </div>
  </div>
</div>

  <!-- form action="">
    <label>Name</label>
    <input type="text">
    <br>
    <label>Email</label>
    <input type="text">
    <br>
    <label>Comments</label>
    <input type="text">
    <br>
    <input type="submit">
  </form -->